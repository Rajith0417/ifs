import { PublishedFilterPipe } from './published-filter.pipe';

describe('PublishedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PublishedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
